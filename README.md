REGULAR EXPRESSIONS
This project is about practicing regular expressions (regex) using Oniguruma, the regex library used by Ruby by default. Other regex libraries (PCRE, POSIX, etc.) sometimes support different features or have different syntax, so the focus here is specifically on how Oniguruma handles pattern matching.
